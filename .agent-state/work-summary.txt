## Work Summary - Bug Fix Iteration

### Fixes Applied

**Fix 1: Pre-generate Interview Clues in engine.ts**
- Added interview clue generation in `generateMystery()` function
- For each suspect (excluding victim), 3 interview clues are now pre-generated with IDs `interview-${suspect.id}-${index}`
- Interview clues are spread into the returned `clues` array with `revealed: false`
- This ensures EvidenceBoard can filter them correctly when GameShell dispatches DISCOVER_CLUES

**Fix 2: Search Button Condition in GameShell.tsx**
- Changed `isNewRoom` condition from `!visitedRooms.includes(currentRoom!)` to `roomClues.some(c => !discoveredClues.includes(c.id))`
- Search button now appears whenever the room has undiscovered clues, not based on visit status
- Better gameplay - button disappears once all clues in the room are discovered

### Verification
```
✓ grep "interview-" lib/game/engine.ts - found at line 181
✓ grep "isNewRoom" components/GameShell.tsx - found at line 277 with correct condition
✓ npm run build - compiled successfully
```

### Results
Both critical bugs are now fixed. The build passes without errors.

---

## Revision 2 — Critical Bug Fixes (Mar 22, 2026)

### Critical Bug 1 Fixed: Interview clues now pre-generated
- Added interview clue generation in `lib/game/engine.ts` inside `generateMystery()`
- 21 interview clues created (7 suspects × 3 questions each, excluding victim)
- Clue IDs match the format `interview-${suspectId}-${index}` used in GameShell's handleQuestionAsked
- Interview clues now exist in `mystery.clues` array, so EvidenceBoard filter finds them

### Critical Bug 2 Fixed: Search button now appears correctly
- Changed `isNewRoom` condition from `!visitedRooms.includes(currentRoom!)` to `roomClues.some(c => !discoveredClues.includes(c.id))`
- Search button now shows whenever room has undiscovered clues
- Button disappears naturally once all clues in room are found

### Verification
- ✓ Build passes (8.63 kB page, 95.9 kB first load)
- ✓ Interview clues pre-generated: `grep -n "interview-" lib/game/engine.ts` shows generation code
- ✓ isNewRoom fixed: `grep -n "isNewRoom" components/GameShell.tsx` shows new condition

### Total Clues Per Mystery
- 3 direct clues (murder weapon, witness, suspect location)
- 3 herring clues (innocent suspects acting suspicious)
- 1 alibi hole clue
- 8 physical room clues (one per room)
- 21 interview clues (7 suspects × 3 questions)
- **Total: 36 clues per mystery**
